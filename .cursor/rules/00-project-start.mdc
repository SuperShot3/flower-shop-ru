---
description: Main project instructions for EKB Flowers (Russia)
alwaysApply: true
---

Before working on this repository, read:

1. `/ai_context/00_START_HERE.md`
2. The relevant `/ai_context/` file for the current task

Do not rely on assumptions or memory. Inspect the actual code before editing.

This is **EKB Flowers** — Russia storefront at [ekb-flowers.ru](https://www.ekb-flowers.ru). **Isolated** from Thailand Lanna Bloom ([lannabloom.shop](https://lannabloom.shop)).

**MVP stack:** Next.js on Vercel, Neon Postgres, Vercel Blob, domain at REG.RU. Payments disabled until YooKassa. Admin seed: `k.v.polovnikov@gmail.com`.

**Forbidden in Russia runtime env:** `SUPABASE_*`, `STRIPE_*`, `NEXT_PUBLIC_GTM_ID`, `RESEND_API_KEY`.

**Later:** Timeweb VPS — see `docs/deploy-vps.md`.

For product or blog copy, use `.cursor/skills/flower-content-writer/` or `.cursor/skills/blog-content-writer/` instead of expanding rules.
