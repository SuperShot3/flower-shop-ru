---
description: Rules for analytics and conversion tracking
globs:
  - "lib/analytics/**"
  - "lib/analytics.ts"
  - "components/GoogleAnalytics.tsx"
  - "app/**/checkout/**"
  - "app/order/**"
alwaysApply: false
---

Before editing analytics or conversion tracking, read:

- `/ai_context/05_ANALYTICS_GTM_GA4_ADS.md`

**Russia (EKB Flowers):** Use **Yandex Metrica** (`NEXT_PUBLIC_YANDEX_METRICA_ID`). Do **not** set `NEXT_PUBLIC_GTM_ID` — forbidden in Russia env.

**Legacy Thailand:** GTM → GA4 via `dataLayer` only. `purchase` after confirmed Stripe payment. Not used in Russia MVP.
